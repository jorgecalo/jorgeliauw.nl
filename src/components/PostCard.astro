---
import type { CollectionEntry } from 'astro:content';

interface Props {
  post: CollectionEntry<'posts'>;
}

const { post } = Astro.props;
const { title, description, pubDate, heroImage, tags } = post.data;

const formattedDate = new Intl.DateTimeFormat('en-US', {
  year: 'numeric',
  month: 'short',
  day: 'numeric',
}).format(pubDate);

const readingTimeMinutes = Math.max(1, Math.ceil((post.body?.split(/\s+/).length || 200) / 200));
---

<article class="group relative flex flex-col bg-slate-900/60 border border-slate-800/80 hover:border-cyan-500/50 rounded-2xl overflow-hidden transition-all duration-300 hover:-translate-y-1 hover:shadow-xl hover:shadow-cyan-500/10">
  <a href={`/posts/${post.slug}/`} class="block aspect-[16/9] overflow-hidden bg-slate-950 relative">
    <img
      src={heroImage}
      alt={title}
      loading="lazy"
      class="w-full h-full object-cover object-center group-hover:scale-105 transition-transform duration-500"
    />
    <div class="absolute inset-0 bg-gradient-to-t from-slate-950 via-transparent to-transparent opacity-60"></div>
    <div class="absolute top-3 left-3 flex flex-wrap gap-1.5">
      {tags.slice(0, 2).map((tag) => (
        <span class="px-2.5 py-1 rounded-md text-xs font-mono font-medium bg-slate-950/85 text-cyan-400 border border-cyan-500/30 backdrop-blur-md">
          {tag}
        </span>
      ))}
    </div>
  </a>

  <div class="p-6 flex flex-col flex-grow">
    <div class="flex items-center gap-3 text-xs font-mono text-slate-400 mb-3">
      <time datetime={pubDate.toISOString()}>{formattedDate}</time>
      <span>&bull;</span>
      <span>{readingTimeMinutes} min read</span>
    </div>

    <h3 class="text-xl font-bold text-white group-hover:text-cyan-400 transition-colors leading-snug mb-3">
      <a href={`/posts/${post.slug}/`} class="focus:outline-none">
        <span class="absolute inset-0" aria-hidden="true"></span>
        {title}
      </a>
    </h3>

    <p class="text-sm text-slate-400 line-clamp-3 mb-4 flex-grow">
      {description || 'Read this technical article on Google Cloud and Cloud Security by Jorge Liauw Calo.'}
    </p>

    <div class="flex items-center text-xs font-semibold text-cyan-400 group-hover:text-cyan-300 mt-auto pt-3 border-t border-slate-800/60">
      <span>Read article</span>
      <svg class="w-4 h-4 ml-1.5 group-hover:translate-x-1 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6" />
      </svg>
    </div>
  </div>
</article>
